// interactiveMain.ts
// Main file to start the interactive console interface

import ConsoleInterface from "./consoleInterface";

function startInteractiveDemo(): void {
  const consoleApp = new ConsoleInterface();
  consoleApp.start();
}

// Start the interactive demo
startInteractiveDemo();
